export function SquaredImg({ imgSrc, cls }) {
  return <img src={imgSrc} alt={imgSrc} className={` ${cls}`} />;
}
export function roundedImg({ imgSrc, cls }) {
  return <img src={imgSrc} alt={imgSrc} className={` ${cls}`} />;
}
